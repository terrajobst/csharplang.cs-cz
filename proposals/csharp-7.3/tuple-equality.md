---
ms.openlocfilehash: f238a711e710bbac7f5b7400fa938bd85dec00c6
ms.sourcegitcommit: 5278336b61519956240a6f7d83bcb4322019e032
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/10/2020
ms.locfileid: "79485250"
---
# <a name="support-for--and--on-tuple-types"></a><span data-ttu-id="9bca2-101">Podpora = = a! = u typů řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="9bca2-101">Support for == and != on tuple types</span></span>

<span data-ttu-id="9bca2-102">Umožňuje výrazy `t1 == t2`, kde `t1` a `t2` jsou řazené kolekce členů nebo typy řazené kolekce členů stejné mohutnosti, a jsou vyhodnoceny přibližně jako `temp1.Item1 == temp2.Item1 && temp1.Item2 == temp2.Item2` (za předpokladu, že `var temp1 = t1; var temp2 = t2;`).</span><span class="sxs-lookup"><span data-stu-id="9bca2-102">Allow expressions `t1 == t2` where `t1` and `t2` are tuple or nullable tuple types of same cardinality, and evaluate them roughly as `temp1.Item1 == temp2.Item1 && temp1.Item2 == temp2.Item2` (assuming `var temp1 = t1; var temp2 = t2;`).</span></span>

<span data-ttu-id="9bca2-103">Naopak umožní `t1 != t2` a vyhodnotit ho jako `temp1.Item1 != temp2.Item1 || temp1.Item2 != temp2.Item2`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-103">Conversely it would allow `t1 != t2` and evaluate it as `temp1.Item1 != temp2.Item1 || temp1.Item2 != temp2.Item2`.</span></span>

<span data-ttu-id="9bca2-104">V případě možného případu s možnou hodnotou null se použijí další kontroly `temp1.HasValue` a `temp2.HasValue`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-104">In the nullable case, additional checks for `temp1.HasValue` and `temp2.HasValue` are used.</span></span> <span data-ttu-id="9bca2-105">`nullableT1 == nullableT2` například vyhodnotí jako `temp1.HasValue == temp2.HasValue ? (temp1.HasValue ? ... : true) : false`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-105">For instance, `nullableT1 == nullableT2` evaluates as `temp1.HasValue == temp2.HasValue ? (temp1.HasValue ? ... : true) : false`.</span></span>

<span data-ttu-id="9bca2-106">Vrátí-li se v porovnání s prvkem výsledek nebool (například při použití nelogického `operator ==` nebo `operator !=`, nebo v dynamickém porovnání), pak bude tento výsledek převeden na `bool` nebo prostřednictvím `operator true` nebo `operator false`, aby mohl získat `bool`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-106">When an element-wise comparison returns a non-bool result (for instance, when a non-bool user-defined `operator ==` or `operator !=` is used, or in a dynamic comparison), then that result will be either converted to `bool` or run through `operator true` or `operator false` to get a `bool`.</span></span> <span data-ttu-id="9bca2-107">Porovnání řazené kolekce členů vždy ukončí vrácení `bool`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-107">The tuple comparison always ends up returning a `bool`.</span></span>

<span data-ttu-id="9bca2-108">Od C# 7,2, tento kód vytvoří chybu (`error CS0019: Operator '==' cannot be applied to operands of type '(...)' and '(...)'`), pokud není k dispozici `operator==`definovaný uživatelem.</span><span class="sxs-lookup"><span data-stu-id="9bca2-108">As of C# 7.2, such code produces an error (`error CS0019: Operator '==' cannot be applied to operands of type '(...)' and '(...)'`), unless there is a user-defined `operator==`.</span></span>

## <a name="details"></a><span data-ttu-id="9bca2-109">Podrobnosti</span><span class="sxs-lookup"><span data-stu-id="9bca2-109">Details</span></span>

<span data-ttu-id="9bca2-110">Při vytváření vazby operátoru `==` (nebo `!=`) jsou stávající pravidla: (1) dynamický případ, (2) rozlišení přetěžování a (3) selže.</span><span class="sxs-lookup"><span data-stu-id="9bca2-110">When binding the `==` (or `!=`) operator, the existing rules are: (1) dynamic case, (2) overload resolution, and (3) fail.</span></span>
<span data-ttu-id="9bca2-111">Tento návrh přidá případ řazené kolekce členů mezi (1) a (2): jsou-li oba operandy relačního operátoru řazené kolekce členů (mají typy řazené kolekce členů nebo jsou literály řazené kolekce členů) a mají shodné mohutnosti, je provedeno porovnání na základě prvku.</span><span class="sxs-lookup"><span data-stu-id="9bca2-111">This proposal adds a tuple case between (1) and (2): if both operands of a comparison operator are tuples (have tuple types or are tuple literals) and have matching cardinality, then the comparison is performed element-wise.</span></span> <span data-ttu-id="9bca2-112">Rovnost řazené kolekce členů je také přetažena na napouštějící řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="9bca2-112">This tuple equality is also lifted onto nullable tuples.</span></span>

<span data-ttu-id="9bca2-113">Oba operandy (a v případě literálů řazené kolekce členů, jejich prvky) jsou vyhodnocovány v pořadí zleva doprava.</span><span class="sxs-lookup"><span data-stu-id="9bca2-113">Both operands (and, in the case of tuple literals, their elements) are evaluated in order from left to right.</span></span> <span data-ttu-id="9bca2-114">Každý pár prvků se pak použije jako operandy k navázání operátoru `==` (nebo `!=`) rekurzivně.</span><span class="sxs-lookup"><span data-stu-id="9bca2-114">Each pair of elements is then used as operands to bind the operator `==` (or `!=`), recursively.</span></span> <span data-ttu-id="9bca2-115">Všechny prvky s typem kompilace, `dynamic` způsobují chybu.</span><span class="sxs-lookup"><span data-stu-id="9bca2-115">Any elements with compile-time type `dynamic` cause an error.</span></span> <span data-ttu-id="9bca2-116">Výsledky těchto porovnání prvků jsou používány jako operandy v řetězu operátorů podmíněná a (nebo).</span><span class="sxs-lookup"><span data-stu-id="9bca2-116">The results of those element-wise comparisons are used as operands in a chain of conditional AND (or OR) operators.</span></span>

<span data-ttu-id="9bca2-117">Například v kontextu `(int, (int, int)) t1, t2;``t1 == (1, (2, 3))` vyhodnocen jako `temp1.Item1 == temp2.Item1 && temp1.Item2.Item1 == temp2.Item2.Item1 && temp2.Item2.Item2 == temp2.Item2.Item2`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-117">For instance, in the context of `(int, (int, int)) t1, t2;`, `t1 == (1, (2, 3))` would evaluate as `temp1.Item1 == temp2.Item1 && temp1.Item2.Item1 == temp2.Item2.Item1 && temp2.Item2.Item2 == temp2.Item2.Item2`.</span></span>

<span data-ttu-id="9bca2-118">Pokud je literál řazené kolekce členů použit jako operand (na obou stranách), obdrží převedený typ řazené kolekce členů vytvořený převody prvků, které jsou zavedeny při vázání operátoru `==` (nebo `!=`) prvku.</span><span class="sxs-lookup"><span data-stu-id="9bca2-118">When a tuple literal is used as operand (on either side), it receives a converted tuple type formed by the element-wise conversions which are introduced when binding the operator `==` (or `!=`) element-wise.</span></span> 

<span data-ttu-id="9bca2-119">Například v `(1L, 2, "hello") == (1, 2L, null)`je převedený typ pro literály řazené kolekce členů `(long, long, string)` a druhý literál nemá žádný přirozený typ.</span><span class="sxs-lookup"><span data-stu-id="9bca2-119">For instance, in `(1L, 2, "hello") == (1, 2L, null)`, the converted type for both tuple literals is `(long, long, string)` and the second literal has no natural type.</span></span>


### <a name="deconstruction-and-conversions-to-tuple"></a><span data-ttu-id="9bca2-120">Dekonstrukce a převody na řazenou kolekci členů</span><span class="sxs-lookup"><span data-stu-id="9bca2-120">Deconstruction and conversions to tuple</span></span>
<span data-ttu-id="9bca2-121">V `(a, b) == x`skutečnost, že `x` lze dekonstruovat do dvou prvků, nehraje roli.</span><span class="sxs-lookup"><span data-stu-id="9bca2-121">In `(a, b) == x`, the fact that `x` can deconstruct into two elements does not play a role.</span></span> <span data-ttu-id="9bca2-122">To může být vhodné v budoucím návrhu, i když by to vyvolalo otázky týkající se `x == y` (Jedná se o jednoduché porovnání nebo porovnání s prvkem), a pokud ano, pomocí kterého mohutnosti?).</span><span class="sxs-lookup"><span data-stu-id="9bca2-122">That could conceivably be in a future proposal, although it would raise questions about `x == y` (is this a simple comparison or an element-wise comparison, and if so using what cardinality?).</span></span>
<span data-ttu-id="9bca2-123">Podobně převody na řazené kolekce členů nehrají žádné role.</span><span class="sxs-lookup"><span data-stu-id="9bca2-123">Similarly, conversions to tuple play no role.</span></span>

### <a name="tuple-element-names"></a><span data-ttu-id="9bca2-124">Názvy elementů řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="9bca2-124">Tuple element names</span></span>

<span data-ttu-id="9bca2-125">Při převodu literálu řazené kolekce členů se zobrazí upozornění, když se v literálu poskytl explicitní název prvku řazené kolekce členů, ale neshoduje se s názvem elementu cílové řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="9bca2-125">When converting a tuple literal, we warn when an explicit tuple element name was provided in the literal, but it doesn't match the target tuple element name.</span></span>
<span data-ttu-id="9bca2-126">V porovnání řazené kolekce členů používáme stejné pravidlo, takže předpokládáme `(int a, int b) t` `d` v `t == (c, d: 0)`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-126">We use the same rule in tuple comparison, so that assuming `(int a, int b) t` we warn on `d` in `t == (c, d: 0)`.</span></span>

### <a name="non-bool-element-wise-comparison-results"></a><span data-ttu-id="9bca2-127">Nebool – výsledky porovnávacích prvků</span><span class="sxs-lookup"><span data-stu-id="9bca2-127">Non-bool element-wise comparison results</span></span>

<span data-ttu-id="9bca2-128">Pokud je v rovnosti řazené kolekce členů dynamické porovnání elementu, používáme dynamické vyvolání operátoru `false` a negaci, které získá `bool` a pokračuje s dalšími porovnávacími prvky.</span><span class="sxs-lookup"><span data-stu-id="9bca2-128">If an element-wise comparison is dynamic in a tuple equality, we use a dynamic invocation of the operator `false` and negate that to get a `bool` and continue with further element-wise comparisons.</span></span> 

<span data-ttu-id="9bca2-129">Pokud se v rovnosti řazené kolekce členů vrátí nějaký typ porovnání elementu, existují dva případy:</span><span class="sxs-lookup"><span data-stu-id="9bca2-129">If an element-wise comparison returns some other non-bool type in a tuple equality, there are two cases:</span></span>
- <span data-ttu-id="9bca2-130">Pokud se typ, který není bool, převede na `bool`, použijeme tento převod.</span><span class="sxs-lookup"><span data-stu-id="9bca2-130">if the non-bool type converts to `bool`, we apply that conversion,</span></span>
- <span data-ttu-id="9bca2-131">Pokud žádný takový převod neexistuje, ale typ má operátor `false`, použijeme tento výsledek a negaci.</span><span class="sxs-lookup"><span data-stu-id="9bca2-131">if there is no such conversion, but the type has an operator `false`, we'll use that and negate the result.</span></span>

<span data-ttu-id="9bca2-132">V nerovnosti řazené kolekce členů se stejná pravidla použijí s výjimkou toho, že použijeme operátor `true` (bez negace) namísto operátoru `false`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-132">In a tuple inequality, the same rules apply except that we'll use the operator `true` (without negation) instead of the operator `false`.</span></span>

<span data-ttu-id="9bca2-133">Tato pravidla jsou podobná pravidlům pro použití nebool typu v příkazu `if` a některých dalších existujících kontextech.</span><span class="sxs-lookup"><span data-stu-id="9bca2-133">Those rules are similar to the rules involved for using a non-bool type in an `if` statement and some other existing contexts.</span></span>

## <a name="evaluation-order-and-special-cases"></a><span data-ttu-id="9bca2-134">Pořadí vyhodnocení a speciální případy</span><span class="sxs-lookup"><span data-stu-id="9bca2-134">Evaluation order and special cases</span></span>
<span data-ttu-id="9bca2-135">Hodnota na levé straně se vyhodnocuje jako první, pak na pravé straně, pak na základě porovnání elementů zleva doprava (včetně převodů a při předčasném ukončení na základě stávajících pravidel pro podmíněné a/nebo operátory).</span><span class="sxs-lookup"><span data-stu-id="9bca2-135">The left-hand-side value is evaluated first, then the right-hand-side value, then the element-wise comparisons from left to right (including conversions, and with early exit based on existing rules for conditional AND/OR operators).</span></span>

<span data-ttu-id="9bca2-136">Například pokud existuje převod typu `A` na typ `B` a metoda `(A, A) GetTuple()`, vyhodnocování `(new A(1), (new B(2), new B(3))) == (new B(4), GetTuple())` znamená:</span><span class="sxs-lookup"><span data-stu-id="9bca2-136">For instance, if there is a conversion from type `A` to type `B` and a method `(A, A) GetTuple()`, evaluating `(new A(1), (new B(2), new B(3))) == (new B(4), GetTuple())` means:</span></span>
- `new A(1)`
- `new B(2)`
- `new B(3)`
- `new B(4)`
- `GetTuple()`
- <span data-ttu-id="9bca2-137">pak se vyhodnotí převody a porovnání podle prvků a podmíněná logika (převést `new A(1)` na typ `B`a pak je porovnejte s `new B(4)`a tak dále).</span><span class="sxs-lookup"><span data-stu-id="9bca2-137">then the element-wise conversions and comparisons and conditional logic is evaluated (convert `new A(1)` to type `B`, then compare it with `new B(4)`, and so on).</span></span>

### <a name="comparing-null-to-null"></a><span data-ttu-id="9bca2-138">Porovnávání `null` `null`</span><span class="sxs-lookup"><span data-stu-id="9bca2-138">Comparing `null` to `null`</span></span>

<span data-ttu-id="9bca2-139">Jedná se o zvláštní případ z normálního porovnání, které se přenáší na porovnání řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="9bca2-139">This is a special case from regular comparisons, that carries over to tuple comparisons.</span></span> <span data-ttu-id="9bca2-140">Je povoleno porovnání `null == null` a literály `null` nezískají žádný typ.</span><span class="sxs-lookup"><span data-stu-id="9bca2-140">The `null == null` comparison is allowed, and the `null` literals do not get any type.</span></span>
<span data-ttu-id="9bca2-141">V rovnosti řazené kolekce členů to znamená, `(0, null) == (0, null)` je také povoleno a `null` a řazené kolekce členů nezískají typ ani jednu z těchto znaků.</span><span class="sxs-lookup"><span data-stu-id="9bca2-141">In tuple equality, this means, `(0, null) == (0, null)` is also allowed and the `null` and tuple literals don't get a type either.</span></span>

### <a name="comparing-a-nullable-struct-to-null-without-operator"></a><span data-ttu-id="9bca2-142">Porovnávání struktury s možnou hodnotou null a `null` bez `operator==`</span><span class="sxs-lookup"><span data-stu-id="9bca2-142">Comparing a nullable struct to `null` without `operator==`</span></span>

<span data-ttu-id="9bca2-143">Jedná se o další zvláštní případ z normálního porovnání, které se přenáší na porovnání řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="9bca2-143">This is another special case from regular comparisons, that carries over to tuple comparisons.</span></span>
<span data-ttu-id="9bca2-144">Pokud máte `struct S` bez `operator==`, je povolené porovnání `(S?)x == null` a je interpretováno jako `((S?).x).HasValue`.</span><span class="sxs-lookup"><span data-stu-id="9bca2-144">If you have a `struct S` without `operator==`, the `(S?)x == null` comparison is allowed, and it is interpreted as `((S?).x).HasValue`.</span></span>
<span data-ttu-id="9bca2-145">V rovnosti řazené kolekce členů se používá stejné pravidlo, takže `(0, (S?)x) == (0, null)` je povolená.</span><span class="sxs-lookup"><span data-stu-id="9bca2-145">In tuple equality, the same rule is applied, so `(0, (S?)x) == (0, null)` is allowed.</span></span>

## <a name="compatibility"></a><span data-ttu-id="9bca2-146">Kompatibilita</span><span class="sxs-lookup"><span data-stu-id="9bca2-146">Compatibility</span></span>

<span data-ttu-id="9bca2-147">Pokud někdo zapsal vlastní `ValueTuple` typy s implementací relačního operátoru, dříve byl dříve vyzvednutý řešením přetížení.</span><span class="sxs-lookup"><span data-stu-id="9bca2-147">If someone wrote their own `ValueTuple` types with  an implementation of the comparison operator, it would have previously been picked up by overload resolution.</span></span> <span data-ttu-id="9bca2-148">Ale vzhledem k tomu, že nový případ řazené kolekce členů pochází před řešením přetížení, bychom tento případ poznamenali s porovnáním řazené kolekce členů namísto spoléhání na porovnávání definované uživatelem.</span><span class="sxs-lookup"><span data-stu-id="9bca2-148">But since the new tuple case comes before overload resolution, we would handle this case with tuple comparison instead of relying on the user-defined comparison.</span></span>

----

<span data-ttu-id="9bca2-149">Vztah k [operátorům testování relačních a typů](../../spec/expressions.md#relational-and-type-testing-operators) má vztah k [#190](https://github.com/dotnet/csharplang/issues/190)</span><span class="sxs-lookup"><span data-stu-id="9bca2-149">Relates to [relational and type testing operators](../../spec/expressions.md#relational-and-type-testing-operators) Relates to [#190](https://github.com/dotnet/csharplang/issues/190)</span></span>
